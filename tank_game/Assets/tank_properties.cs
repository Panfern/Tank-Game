using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class tank_properties : MonoBehaviour
{
    // Start is called before the first frame update
    public int tank_number;
    public double zAxis;
    void Start()
    {
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
